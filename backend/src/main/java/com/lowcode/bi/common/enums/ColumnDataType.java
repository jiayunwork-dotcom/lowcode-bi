package com.lowcode.bi.common.enums;

public enum ColumnDataType {
    STRING,
    INTEGER,
    LONG,
    DOUBLE,
    DECIMAL,
    BOOLEAN,
    DATE,
    DATETIME,
    TIMESTAMP,
    TEXT,
    BINARY,
    JSON
}
